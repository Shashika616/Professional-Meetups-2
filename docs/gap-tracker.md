# Gap tracker

Living record of known gaps, opened 2026-09-08 from a full UX + backend
architecture review. Not vault-synced — hand-maintained here, like
`docs/decisions/` and `docs/plans/`. Update status inline as items close;
don't delete closed rows, mark them `DONE (date)` so the history stays
legible.

Columns: **Gap** — **Where** — **Why not fixed yet** — **Status**.

## Frontend / UX

1. **Privacy Controls row is a silent dead tap** - `frontend/lib/features/profile/profile_page.dart:156-165`, no `onTap` handler despite looking tappable (chevron + subtitle). - Never audited before; no prior round did a full dead-tap sweep of Profile. - **DONE (2026-09-10).** `PrivacyControlsPage` created and wired to the row. Ships deliberately with *no* toggles: it states what the app does today and says the controls are coming, because a privacy switch that renders but changes nothing is the exact failure the live-location opt-in already had once (`meetup/safety.go`'s ShareWithContacts: "a safety feature that only appeared to work, which is worse than not offering one"). Two neighbouring dead-tap causes fixed in the same pass: every `_PrefRow`/verification row was `GestureDetector` with the default `deferToChild`, so only the painted label and chevron took the hit and the gap between them was dead - now `HitTestBehavior.opaque`; and the Notifications row, previously a "SOON" placeholder, now opens the real notifications page.

2. **Accessibility near-absent** — one `Semantics` usage in the whole app (`events_page.dart:499`), several tap targets under 44/48dp, a `TextButton` minimum size deliberately dropped in `onboarding_flow.dart:381-384` for layout convenience. — Accessibility was never the stated lens of any prior review round. — **OPEN**.

3. **`dating`/`rideShare` shown as live, unlocked chips to signed-out users** — `frontend/lib/features/landing/widgets/orbiting_intents.dart:21-28`. — The underlying Play Store age-verification policy question is legitimately blocked on a business decision (see `docs/07-research/app-store-and-play-store-compliance.md` in the source repo), but the narrower fix — not showing these as live/unlocked pre-auth — doesn't depend on that decision and was never split out. — **OPEN, partially actionable now**.

4. **Stale design tokens** — `glassTint`/`glassBorder` still defined in `core/theme/app_palette.dart:48-50` after ADR-032 moved the app off glassmorphism; a second unofficial `Colors.white.withValues(...)` convention living alongside the real token system (e.g. `profile_page.dart:781`, `intent_filter_bar.dart:123`, `schedule_flow.dart:792`). — ADR-032's implementation was verified for functional correctness only, not audited for leftover dead tokens afterward. — **OPEN**.

5. **130 lines of commented-out fallback `_LocationStep`** — `frontend/lib/features/meetups/schedule_flow.dart:578-709`. — Never flagged; minor. — **OPEN**.

6. **`_EditNameDialog` has no input validation** — `frontend/lib/features/profile/profile_page.dart:848-899`, SAVE always enabled even for empty/whitespace input. — Never spot-checked; past form-UX checks focused on OTP/onboarding/scheduling only. — **OPEN**.

7. **CORRECTED (2026-09-08) — this was wrong, not a real gap.** Originally logged as "push notifications fully scaffolded but inert (NoOp)." Re-checked directly: `frontend/lib/core/providers/app_providers.dart:114-122` binds `pushNotificationServiceProvider` to the real `FirebasePushNotificationService` (real Firebase project `professional-meetups-976d2`, real `google-services.json`, token refresh wired to `registerDeviceToken`) — push is genuinely live, done in round-10, in this repo. The actual (much smaller) gap: `frontend/lib/app_shell.dart:58-64,89-90`'s doc comments still say "NoOpPushNotificationService.messages never emits," left over from round-9 scaffolding and never updated when the binding was swapped to real Firebase. Misleading stale comments, not a missing feature. — **OPEN — doc-comment cleanup only**.

15. **"Told N trusted contacts" confirmation never displays** — `frontend/lib/core/models/meetup.dart:410-419`, `SafetyState.fromJson` never reads `shared_with_contact_ids` from the server response, so it silently defaults to empty on every real HTTP call. Found 2026-09-08 verifying the trusted-contact-share feature (`docs/plans/10-safety-gate-audit-and-contact-share.md`) — backend fully correct, this one frontend field mapping was missed, and no test caught it because all widget tests construct `SafetyState` directly rather than through `fromJson`. Fix handed off: `docs/plans/11-safety-share-confirmation-parsing-fix.md`. **OPEN — fix ready to run.**
16. **`live_location_opt_in` field/RPC now dead, left in place on purpose** — `gateway/handlers/meetups.go:434-446`, `grpcapi/meetup.go:318-319`. Superseded by the trusted-contact-share feature; self-flagged in the completion report as a deliberate deferral (avoid churning the proto in the same pass), not an oversight. **DEFERRED, tracked — remove in a dedicated cleanup pass.**

## Backend

8. **No billing/subscription module in the monolith** — ADR-031 + Slice B billing service were designed, built, and verified in the original microservices repo; no equivalent module exists under `backend/internal/modules/` in this repo. — Same root cause as #7: phased port never scheduled a billing phase. — **OPEN — porting gap**.

9. **Stadia Maps API key committed in plaintext** — `frontend/.env:37`. — Already tracked in `TESTING-NOTES.md` as a "must not ship" item, but only the vendor-choice question was tracked as open; the independent, cheap fix (rotate + stop committing) was never separated out and actioned. — **OPEN — fix this independently of the vendor decision**.

10. **Hardcoded OTP bypass (`123456`)** — `backend/.../otp.go:88-93`, gated by `ALLOW_TEST_OTP_BYPASS`, absent from `.env.example`, loud `WARN` on boot when active. — Correctly gated as a testing aid, tracked in `TESTING-NOTES.md`. Recommended hardening never built: refuse to boot if the flag is true in a production environment, rather than relying on a log line being noticed. — **OPEN — hardening, not a leak**.

11. **Validation is hand-rolled per-field regex, no shared schema/DTO layer** — e.g. `backend/internal/modules/auth/validate.go:29-56`. — New recommendation; past reviews confirmed validation existed per-route, never audited the pattern's structural consistency as module count grows. — **OPEN**.

12. **No distributed tracing** — no OpenTelemetry/trace spans anywhere in `backend/internal/`. — New observation; past operational-readiness audits covered logging/metrics/shutdown/migrations, not tracing. — **OPEN**.

13. **OTP verify endpoints lack a target-keyed rate limit** (send-side has one; verify relies on blanket IP+path limiting plus the 5-attempt code lockout) — `backend/internal/platform/ratelimit/`, `backend/.../otp.go:20-25`. — Past rate-limit audit checked send-side limits existed; never drilled into send/verify asymmetry. Also unconfirmed: whether the 5-attempt lockout can be reset by requesting a fresh code, which would weaken the bound. — **OPEN — needs one explicit check**.

14. **No tamper-evident audit log for admin/moderation actions.** — No admin surface exists yet, so correctly deferred — but must be a hard gate before any admin/moderation tooling ships. — **DEFERRED, not forgotten**.

## Round 2 — full architecture/optimization sweep (2026-09-08)

Dispatched two independent, from-scratch reviews (backend, frontend) explicitly told to skip everything already listed above and find new territory only. One finding (#17) is a genuine correctness bug, verified directly against source, not just accepted from the report.

### Backend

17. **Trust-level lost-update race across independent verification steps — real correctness bug, verified.** `backend/internal/modules/auth/verification.go`'s `VerifyPhoneCode` (67-87), `VerifyPersonalEmailCode` (105-125), and `SubmitPersonalDetails` (130-154) each do read-then-compute-then-write with no transaction spanning the two and no lock: fetch the user row, build a Go-side "hypothetical" copy with only *this* field added, compute `trust_level` from that snapshot, then `UPDATE` the real field plus that precomputed `trust_level`. If two of a user's Level-2 steps complete inside the same short window (very plausible — a user clicking through the verification checklist quickly), each computes trust level from a snapshot that can't see the other's not-yet-committed field. Both underlying fields end up correctly persisted, but `trust_level` gets stamped by whichever write commits last, computed as if only its own field existed — silently under-granting the user's trust level, with nothing to ever recompute it afterward. This is the exact bug class already found and fixed for the meetups-completed cache (`queries/users.sql:135-151`, value-guard not timestamp-guard) — that fix was never generalized to these three `UpdateUser*` call sites, which have no guard at all. On closer design pass, the identical shape (precomputed `trustLevel int`, no lock) also exists in `UpdateWorkEmailVerified`, `UpdateLinkedInSub`, and `ClearGuestFlag` — same bug, three more call sites. **Fix designed and handed off 2026-09-08: `docs/plans/12-trust-level-race-fix.md` + `docs/plans/12-trust-level-race-fix-claude-code-prompt.md`.** Chosen fix is a `SELECT ... FOR UPDATE` row lock inside a transaction (matching the existing `refresh_tokens_postgres.go` `Rotate` idiom) with `trust_level` recomputed from the locked row via a callback, not a SQL-side `CASE` — keeps `computeTrustLevel`'s logic in exactly one place (Go), not duplicated into SQL. Covers all six affected methods in one pass. **DONE (2026-09-09) — verified against actual source, not just the completion report.** Confirmed directly: all six repository methods now lock via a new `GetUserByIDForUpdate` (`SELECT ... FOR UPDATE`) inside a transaction, apply their own field(s) to the locked snapshot, invoke the caller's callback, write and commit before publishing. All seven service-layer call sites (four in `verification.go`, three in `identity_resolution.go` — including `SignUpOrRecoverWithEmail`'s `UpdatePersonalEmail` call, which neither plan document named but was caught by treating the compiler as authoritative) pass the matching closure. Six fakes rewritten to mutate their own row before invoking the callback, so existing tests exercise real logic, not just a recompiled signature. Three predicted literal-argument test breaks (`identity_resolution_test.go:268,291`, `service_test.go:543`) fixed as predicted. New `trustlevel_race_test.go` (4 tests) reproduces the exact bug shape and passes; a control run confirmed it fails on the old code (`stored trust_level = 1, want 2`). `go build ./...` and `go test ./...` both pass. Honestly-stated limit: a fake repository can't prove the real Postgres row lock serializes concurrent transactions — only a live integration test could — and the report didn't overclaim this.

18. **Missing regression test plus a stale "this is tested" comment on the rating/meetups-completed cache consumers.** `backend/internal/modules/auth/rating_consumer.go:33,55` (`ApplyRatingUpdate`, `ApplyMeetupsCompletedUpdate`) have zero calling test anywhere in the repo. `fakes_test.go:262-264`'s own comment claims a test "exercise[s] this directly" — it doesn't exist. The underlying guard was itself corrected same-day (`fakes_test.go:285-290`) with no regression test added to lock the fix in, on exactly the redelivery/out-of-order-event logic ADR-001 calls "real insurance against a handler running twice." **OPEN.**

19. **`ListActiveMeetups` fetches full pages (up to 400 rows, joined) before filtering down to the handful actually shown.** `backend/internal/modules/meetup/service.go:461-469` calls `ListByHost`/`ListRequestedByUser` with the default 200-row page size each, joined against host-display + rating-average, then discards most of it in Go down to just `open`/`full`/unexpired rows. Pushing that filter into SQL would avoid the wasted transfer/join on every pull-to-refresh and app-resume. Low severity (polling was deliberately removed already) but real waste for any host/participant with meetup history. **OPEN — low priority.**

### Frontend

20. **Home's root-level `ref.watch(authSessionProvider)` causes an oversized rebuild.** `frontend/lib/features/home/home_page.dart:103` watches the whole session/profile object at the top of `build()`, in a widget kept alive for the app's lifetime. Only `HomeHeader` actually renders `displayName`/`imageUrl`; `trustLevel` is only used inside closures. Any auth-session change anywhere in the app (profile edit, any verification step, LinkedIn link) rebuilds the entire Home `ListView` — Active Meetups, Happening Soon, the safety tip card — even though none of them read `profile`. Fix: narrow to a `displayProfileProvider` (name/photo only) via `select`, or wrap just `HomeHeader` in its own `Consumer`. **OPEN.**

21. **Four-plus bespoke error/retry widgets instead of one shared component, and one screen with no retry at all.** Separately reimplemented in `happening_soon_section.dart:498-539`, `manage_trusted_contacts_page.dart:456-481`, and `share_with_contacts_sheet.dart:337-351` — each with its own copy/button styling. `meetup_detail_page.dart:405-411` shows a bare `Center(Text(...))` on load failure with no retry button at all, the only page in this set with no way to recover short of leaving the screen. Fix: extract one `ErrorRetryView` in `core/widgets/`, use it everywhere including meetup_detail_page. **OPEN.**

22. **`SkeletonLoader` convention not applied consistently within a single file.** `premium_page.dart:108-110` correctly wraps its status skeleton in `SkeletonLoader`; `premium_page.dart:122`, a few lines below in the same `build()`, renders `SkeletonBox` directly for the plans-loading state — shows the shimmer instantly instead of after the app's standard 180ms delay, inconsistent within the same screen. Fix: wrap line 122 the same way as line 108. **OPEN — small.**

23. **Cross-feature reach into another feature's private widget folder.** `meetup_detail_page.dart:17-18` imports `LockedCardHeader` from `features/home/widgets/meetup_card.dart`; `schedule_flow.dart:14` imports `IntentTile` from `features/home/widgets/intent_tile.dart` — both pull a widget filed under Home's private `widgets/` into the unrelated Meetups feature. Since they're demonstrably shared, they belong in `core/widgets/`; leaving them under Home means a Home refactor can silently break Meetups. **OPEN.**

24. **Test coverage gap on two widgets with real lifecycle/error logic.** `premium_page.dart` (owns a `ChangeNotifier` controller, listener add/remove, retry-less error text) and `share_with_contacts_sheet.dart` (async provider + selection state + its own error row) have no widget test, unlike comparable pages (`profile_page_test.dart`, `events_page_test.dart`, `manage_trusted_contacts_page_test.dart` all exist). **OPEN.**

## Round 3 — full design review, bad-design/architecture lens (2026-09-09)

Two independent from-scratch reviews (backend, frontend), each given the full list of everything already logged above (#1-24) and told to find new territory only, explicitly asked for design smells and architectural issues, not just bugs. I personally re-verified the four most consequential findings directly against source before recording them (#25, #27, #28, #29 below) rather than relaying the reports as-is; #26 is recorded as reported, not independently re-checked line-by-line.

### Backend

25. **`ListRatableParticipants` doesn't apply the same identity-redaction floor `ListMeetupParticipants` deliberately enforces — verified.** `backend/internal/modules/meetup/participants.go:68-100`'s `ListMeetupParticipants` redacts `UserID`/`FullName`/`ProfilePhotoURL`/`TrustLevel` for any viewer below `participantIdentityFloor` (trust level 2), with an explicit, carefully-written rationale: "somebody who cannot join has no reason to need the guest list, and letting them read it would make the app a directory that anyone can scrape by signing up" (`participants.go:7-20`). `ListRatableParticipants` (`rating.go:22-41`) answers the same underlying question — who's on this meetup — for the same viewer/meetup pair, but returns full identities gated only by `IsParticipant`, with no trust-level check and no `RatableParticipant` field to redact against. Confirmed this is reachable, not theoretical: joining a meetup required only trust level 1 before ADR-013 raised the floor to 2, so a participant who joined under the old floor is a legitimate, still-current participant sitting below `participantIdentityFloor` today — exactly the "grandfathered participant" case `GetMeetup`'s own comment (`service.go:359-365`) already accounts for elsewhere. That participant sees full names/photos/trust levels of everyone else on the meetup via `ListRatableParticipants`, on a meetup where `ListMeetupParticipants` would redact them to a bare count. Not a general scraping hole (the `IsParticipant` gate is real and load-bearing, per the method's own comment) — it's a policy the codebase enforces carefully in one place and forgot to apply in a second, structurally identical place. **DONE (2026-09-09) — verified against actual source.** Confirmed the full chain: `meetup.proto`'s `viewer_trust_level` field, regenerated stubs, both `monolithclient` layers, `handlers/meetups.go` sourcing it from the JWT, and `rating.go` zeroing `TrustLevel` on the post-`ratableParticipantsFromRepo` slice (the corrected location) with a clear doc comment on the deliberate partial redaction. New `TestListRatableParticipants_RedactsOnlyTrustLevel_Integration` runs against a real test database (not a fake) and proves both directions: trust level zeroed below the floor while name/ID survive, real value returned at the floor. `go build`/`go test` both pass.

26. **N+1 sequential per-meetup queries inside the lifecycle-sweep's row-locked claim transaction — reported, not independently re-verified line-by-line.** `NotifyStartingSoonSweep` and `AutoCloseSweep`/`queueMeetupClosed` (`service.go:653-680`, `616-642`, `687-704`) loop over up to 100 claimed meetups and call `ListRequestsForMeetup` once per meetup, synchronously, inside the same `SELECT ... FOR UPDATE SKIP LOCKED` transaction that claims them (`meetups_postgres.go:821-875`) — no batched `ListRequestsForMeetups([]ID)` exists. The codebase already found and fixed this exact shape of problem once, for `recomputeMeetupsCompleted` (`meetups_postgres.go:663-683`, moved out of the closing transaction specifically because it scaled with participants × history while holding locks on `meetup.meetups`) — that fix was never generalized here. Extends lock hold time and DB round-trips proportionally to sweep batch size, on a poller that runs every 60s. **OPEN — worth a follow-up read before prioritizing, given the source review's strong track record of getting this codebase's reasoning right elsewhere.**

### Frontend

27. **DONE (2026-09-09) — verified against actual source.** `setState` after dispose in the Add Trusted Contact dialog's error path — real bug, one missing `mounted` check. `if (!mounted) return;` confirmed in place with an explanatory comment. `frontend/lib/features/safety/manage_trusted_contacts_page.dart:328-347`'s `_save()`: the `on AuthException catch (error)` block calls `setState(() => _error = ...)` with no `mounted` guard, while the `finally` block three lines below correctly guards its own `setState` with `if (mounted)`. If the dialog is dismissed while `addTrustedContact` is in flight and it then throws, this asserts/crashes in debug and is undefined behavior in release. Every other async handler in the codebase (`meetup_detail_page.dart`, `host_meetup_controls.dart`, `schedule_flow.dart`) consistently guards every post-await `setState`; this is the one place that pattern was dropped. **OPEN.**

28. **`MeetupSessionExpiredException` is thrown on every meetup-API 401 but never caught anywhere — verified, confirmed via direct grep and read of `app_shell.dart`.** `core/services/http_meetup_service.dart:344` throws it, and its own doc comment (`meetup_service.dart:247-252`) says it means exactly what `AuthService.SessionExpiredException` means. But a repo-wide search for the type turns up only its definition and that one throw site — zero catch sites. Every meetup call site (`meetup_detail_page.dart`, `schedule_flow.dart`, `host_meetup_controls.dart`, `happening_soon_section.dart`, `events_page.dart`, `share_with_contacts_sheet.dart`) catches only the generic `MeetupException` and shows a toast. Contrast with `AuthService.SessionExpiredException`, which five call sites explicitly catch to call `forceSignOut()`. `AppShell`'s own safety-net listener (`app_shell.dart:194-217`) exists specifically for "any authenticated call site that forgot its own `SessionExpiredException` catch" — but it only fires when `authSessionProvider` actually transitions to logged-out, which nothing does for the meetup-service variant. Net effect: a session that dies mid-use of any meetup feature shows a generic, retriable-looking error forever with no path back to sign-in. Related but distinct, and corrected from the original report: the Safety Center's SOS confirm dialog (`safety_page.dart:203-234`) calls `AuthService.triggerSos` (not the meetup service), and its `on AuthException catch` is broad enough to also swallow a `SessionExpiredException` without calling `forceSignOut()` — same failure *pattern* (generic catch, no forced sign-out), but a separate instance on the auth service's own exception, not the same bug as the meetup-service gap. **DONE (2026-09-09) — verified against actual source.** Fixed at 21 sites across 10 files, four more than either plan document named (`participants_page.dart`, `meetup_review_page.dart`, `meetup_review_section.dart`, `participants_strip.dart`) — caught by the implementer's own grep rather than trusting the plan's list, exactly the discipline the plan asked for. Spot-checked several sites directly, including the three `catch (_)` best-effort-load sites: all correctly special-case `MeetupSessionExpiredException` before the generic handler. SOS dialog fix confirmed correctly ordered (`on SessionExpiredException` before `on AuthException catch`). `share_with_contacts_sheet.dart` was correctly dropped from the list — confirmed it doesn't call the meetup service at all.

29. **`ScheduleFlowPage`'s step-back logic only runs on its custom in-app back button — the OS back gesture bypasses it and discards the whole wizard, verified.** `schedule_flow.dart:194-227`'s `_Header` is a plain `Row` (the `Scaffold` has no `appBar:`), and `_goBack()`'s deliberate one-step-back logic is wired only to that `IconButton`'s `onPressed`. Confirmed a repo-wide search for `PopScope`/`WillPopScope` returns zero matches anywhere in the app. That means Android's back gesture/button and iOS's edge-swipe both pop the entire route immediately, discarding whatever the host has entered across the 5-step intent/timing/location/capacity/review wizard, with no confirmation — even from the final Review step. Real, easily-triggered data-loss path on a form with actual backend consequences. **DONE (2026-09-09) — verified against actual source.** `PopScope<void>` confirmed wrapping the `Scaffold`, routed through the existing `_goBack()`. Plan guessed the wrong callback name (`onPopInvokedWithPopScope`, which doesn't exist); implementer correctly used `onPopInvokedWithResult`, the real non-deprecated API for the installed Flutter SDK (3.47.0) — checked the SDK source rather than guessing, as instructed.

## Round 4 — guest-account lifecycle review (2026-09-09)

Surfaced from a user question about what happens to a guest account after
sign-out, not from a scheduled sweep — same reactive pattern the Process
Note below already calls out.

30. **`AddTrustedContact`/`TriggerSOS` have no trust-level gate — any guest (Level 0, never verified) can use both.** Checked directly: both gateway routes require only `h.requireAuth`. Built (ADR-026, in the vault) before the guest/trust-level-0-3 redesign existed, so this isn't a reversed decision — trust level was never considered for these two RPCs at all. Real consequence found while designing guest-account cleanup: `auth.trusted_contacts`/`auth.sos_events` were deliberately built with no real FK to `auth.users` (migration 0001, for module re-extractability), so deleting an abandoned guest that had used either would leave silently orphaned rows behind. **DONE (2026-09-09) — verified directly against source, not just the completion report.** `safetygate.go`'s `requireSafetyFeatureTrustLevel` (floor 2) is called at the top of both `service.go` methods; the full proto→gateway→grpcapi→service chain threading `caller_trust_level` from the JWT (never client-supplied) confirmed end to end. Three new backend tests confirmed genuinely adversarial, not just compiling around the change: sub-floor calls rejected with zero `sos_events` written, the gate runs *before* input validation (so a rejected caller learns nothing about the layer behind it), and floor-and-above succeeds. Frontend `safety_page.dart` gates `_onSosTapped` and the add-contact entry point via `ref.watch(authSessionProvider)` (a bug in an earlier pass used `ref.read` at tap time, which raced `AsyncLoading` to a false "guest" read — caught by the existing widget tests, now fixed).

31. **No cleanup mechanism for abandoned guest accounts — rows accumulate in `auth.users` forever.** A guest account has no credential to re-authenticate with once its refresh token is gone (logout revokes and deletes it locally; nothing else identifies the account), so "Continue as Guest" always creates a fresh account rather than resuming an old one. Checked all six background jobs in `cmd/monolith/main.go` — none touch `auth.users`. Mitigated somewhat by the existing 5/IP/day guest-signup rate limit, but that's IP-keyed (defeated by a proxy pool) and doesn't address ordinary abandonment (reinstalls, uninstalls, one-time tries). **DONE (2026-09-09) — verified directly against source.** `RefreshTokenSweeper.Tick` now runs `SweepAbandonedGuests` right after the token sweep (order confirmed load-bearing and documented: guest eligibility requires zero token rows, so cleanup must run second). New repository method `DeleteAbandonedGuests` confirmed batched like `DeleteExpired`, with three required conditions read directly from the SQL: `is_guest = true` (a hard, one-way exclusion — verified nothing ever flips it back), a real anti-join against `auth.refresh_tokens` (not a derivable stored flag), and `updated_at` past the same `RefreshTokenRetention` window the token sweep already uses. Only safe in this simplified two-condition-plus-retention form because #30 landed first, per ADR-003's Consequences — confirmed that ordering was actually followed. **New gap surfaced during this verification, not yet fixed — see #32.**

32. **Deleting a guest account (new as of #31's fix) can orphan rows in three `meetup` schema tables that also have no FK to `auth.users` by the same deliberate module-boundary design (migration 0002's header, mirroring 0001's).** Verified directly: `meetup.user_display_cache` gets a row for every account via the `user-onboarded` event (`users_postgres.go`'s `Create`, which `GuestSignup` calls, publishes it unconditionally) — so literally every guest has one. `meetup.user_location_cache` (`POST /v1/users/me/location`) and `meetup.device_tokens` (`POST /v1/meetups/device-token`) are both `h.requireAuth`-only routes, reachable by a Level-0 guest with no trust check at all — confirmed both route registrations directly. #30/#31 do NOT close this: #30 only gates the two `auth`-module safety writes, and #31's cleanup deletes the `auth.users` row itself, not these three `meetup`-module rows, which would be left behind pointing at a UUID that no longer exists anywhere. Self-flagged by the implementer rather than found independently, and confirmed real on inspection. Correct fix is a `user-deleted` event the `meetup` module consumes to clean up its own three tables — an ADR-001 module-boundary matter, not a one-line patch, so it wasn't slipped into Part B's scope. **OPEN — not yet designed.**

## Round 5 - live traffic review of the deployed service (2026-09-10)

Both items below were found by reading the deployed Cloud Run request logs,
not the source. Neither is visible from code alone: both look like ordinary
call sites until you see how often they actually fire in production. Same
reactive pattern the Process Note calls out - triggered by a "check for
wasteful API calls" ask, not a scheduled sweep.

33. **`/v1/billing/subscription` was in a permanent retry loop - the single busiest endpoint on the service, with a 100% failure rate.** Billing is Phase 3 and unbuilt, so `handlers/unavailable.go` answers 503 by design. But `http_subscription_service.dart` threw on it, `subscriptionStatusProvider` is watched by `ProfilePage` (a keep-alive tab, so it stays mounted), and Riverpod 3 auto-retries a failed provider with backoff - so a permanent, expected 503 became roughly 8-10 requests/minute per device for as long as the app was open. Cost was not the requests (well inside the free tier) but that a request every few seconds stops Cloud Run ever scaling to zero, so a warm instance was billed for the whole session. **DONE (2026-09-10).** `currentStatus()` now maps 503 to the free/none status the backend already synthesizes for a user who has never purchased anything - `isEntitled` stays false, so nothing is unlocked by it. Confirmed live: one call per session (`04:35:56`, `22:00:52`, `22:05:13`) instead of a continuous stream. Also fixed the "Could not load subscription options right now" error on the Premium screen, which was the same 503 surfacing.

34. **Raw GPS coordinates as a Riverpod cache key caused a refetch on every location read.** `openMeetupsProvider` is a `.family` keyed on `(intent, viewerLat, viewerLng, withinDays)`, fed the unrounded device position. Consumer GPS jitters 3-10m even on a stationary phone, so every reading minted a new key, missed the cache, and refetched an identical list. Seen directly in the logs: `/v1/meetups` at latitudes 6.8705648 / 6.8705815 / 6.8705842 / 6.8705933 within minutes from a phone on a desk, and `intent=lunch` fetched twice 21 seconds apart. Bounded rather than continuous (`_loadLocation()` runs from `initState()`, not a timer), so it cost one wasted round trip per mount of the Happening Soon section. **DONE (2026-09-10) - see ADR-004** for the decision, the numbers, and the two alternatives deliberately not taken (`cos(latitude)` correction, H3 hexagonal indexing) with explicit revisit triggers for each. Note this changes what the server receives, not just the client cache - weighed and accepted in the ADR, and called out there rather than described as a purely client-side change.

## Round 6 — full security/design sweep (2026-09-15)

Two independent from-scratch reviews (backend, frontend), each told to read
everything above first and find new territory only. All four findings below
were personally re-verified against source before being recorded, not
relayed from the reports as-is. Fix designed and handed off same-day:
`docs/plans/18-round6-security-design-fixes.md` +
`docs/plans/18-round6-security-design-fixes-claude-code-prompt.md`.

### Backend

35. **`TEST_OTP_BYPASS_EMAILS`'s entire safety argument is an unenforced convention, not a runtime check — High, verified.** This mechanism (a third OTP-bypass path, alongside `ALLOW_TEST_OTP_BYPASS` and `TEST_OTP_BYPASS_PHONES` from Plans 16/17) was added and deployed without ever being recorded here. `backend/internal/modules/auth/otp.go`'s doc comments claim it's safe because every allowlisted address is on a reserved TLD (`.test`) that can't be a real mailbox — but `parseOTPBypassAllowlist` (`otp.go:109-121`) never checks this, and `main.go`'s boot warning (`main.go:149-156`) logs only a count, so a safe list and an unsafe one look identical in logs. Confirmed `CompleteEmailLogin` is a real passwordless-login RPC, so an allowlisted real address would be a permanent, credential-free account takeover. Today's 8 deployed entries (`service.yaml:139`) are verified all on `.test` — nothing is currently exploited — but the property is enforced by nobody and nothing. **DONE (2026-09-15) — verified directly against source.** New `ValidateTestOTPBypassEmails` (`otp.go`) checks every entry against `reservedTestTLDs` (`.test`, `.example`, `.invalid`, `.localhost`); `main.go` calls it before logging the count and returns an error naming the bad entry, which `main()`'s existing error path turns into `os.Exit(1)` — confirmed this is the same fatal-startup path every other required-config failure already uses, not a new one. New test covers all four reserved suffixes, several deliberately adversarial rejections (a domain that merely *contains* `.example` as a substring rather than ending in it; a bare `test` label with no leading dot), and confirms the error names the specific offending entry. Doc comment updated to say the restriction is enforced, not just stated.

36. **`sos.go` doc comment claims a DB CHECK mirror that no longer exists — Low, verified.** `sos.go:57-58` says phone-or-email is "enforced server-side here, mirroring the DB CHECK constraint," but `sos.go:182-184` now requires phone specifically (a deliberate, justified change) while the DB CHECK (`migrations/0001_auth_schema.up.sql:251`) was never tightened to match. Not exploitable today (Go is the only insert path) but misleading about what backstop exists. **DONE (2026-09-15) — verified.** Comment now states phone is specifically required at the Go layer and the DB CHECK is intentionally looser (legacy, predates the phone-mandatory rule) — accurate, no code/migration touched, exactly as scoped.

### Frontend

37. **Sign-in screen creates new accounts with a hardcoded, never-shown age confirmation — High, verified.** `frontend/lib/features/auth/social_sign_in_section.dart` hardcodes `ageConfirmedOver18: true` on all three provider calls (lines 127/137/147). This widget is shared between the sign-up flow (`onboarding_flow.dart`, which correctly shows `AgeConfirmationStep` first) and the new `email_login_page.dart`'s sign-in method step, reached directly from the landing page's "SIGN IN" text (`landing_page.dart:116-119`) with no age-confirmation step anywhere in that path. Confirmed via `identity_resolution.go:57,98,217` that `ageConfirmedOver18` is a real, enforced legal self-attestation checked at account-creation time, not decorative — a brand-new user who taps "Sign In" rather than "Sign Up" and picks a provider gets an account created with the server recording a confirmation that was never shown. A second, independent instance of the age-verification compliance concern `docs/07-research/app-store-and-play-store-compliance.md` already tracks for the `dating` intent chip (gap #3) — different code path, same underlying exposure. **DONE (2026-09-15) — verified directly against source.** New shared `AgeConfirmationCheckbox` widget (one control, one sentence, used by both screens — the sign-up screen's private checkbox method was deleted, not duplicated) is shown inline on the sign-in method step; `SocialSignInSection` now takes a real `ageConfirmedOver18` param instead of a constant, disables its provider buttons (`onPressed: widget.ageConfirmedOver18 ? onPressed : null`) until checked, and independently refuses inside `_run` too — belt-and-braces, not UI-only. `onboarding_flow.dart` was also updated to thread the step's real recorded value into `guestSignup`/`EmailSignupStep`/`SocialSignInSection` instead of its own separate hardcoded `true`s, closing the same bug shape at every call site, not just the one reported. New tests confirm: an unchecked box blocks the call entirely (zero calls to the fake service), a checked box sends the real value, email OTP login is deliberately *not* gated (it can only reach an existing account), and the sign-up path's existing behavior is unchanged (regression test added). Extracting the shared widget also picked up gap #1's dead-tap fix pattern (`HitTestBehavior.opaque`) for free.

38. **`HomePage` leaks a `listenManual` subscription on every sign-out/sign-in cycle — Medium, verified.** `home_page.dart:94` calls `ref.listenManual(viewerLocationProvider, (_, _) {})` in `initState()` with no `.close()` in `dispose()` (`home_page.dart:97-101` only disposes `_scrollController`). `viewerLocationProvider` is deliberately `autoDispose` specifically so sign-out/sign-in "starts clean" (its own doc comment) — the missing close defeats that on every `HomePage` teardown, leaking one listener per cycle. **DONE (2026-09-15) — verified.** Subscription now held in a typed field (`ProviderSubscription<ViewerLocation>`) and closed in `dispose()` before `_scrollController.dispose()`, matching the existing pattern exactly.

**Verification note**: all four fixes were confirmed by reading the actual diffs line-by-line (not by trusting the completion report), including the new tests' logic. The backend/frontend test suites themselves could not be *executed* independently in this pass (no Go/Flutter toolchain available in the reviewing environment) — the code and test logic read as correct, but treat "all tests pass" as the implementer's claim, confirmed structurally rather than by a second independent run, until you see the actual `go test`/`flutter test` output yourself.

## Round 7 — schedule-conflict + sign-out review (2026-09-15)

Reviewed the user's own newly-added feature (ADR-005: one-meetup-at-a-time,
single-request sign-out, meal-sitting display) — two independent
from-scratch reviews (backend, frontend), lens deliberately different this
time: duplicate logic, inconsistencies, logical errors, memory leaks,
network/battery drain, rather than a security pass. The one real bug was
personally re-verified against source. Fix designed and handed off:
`docs/plans/19-schedule-conflict-race-fix.md` + Claude Code prompt.

### Backend

39. **`checkScheduleConflict` is check-then-act with no lock — two ordinary concurrent requests defeat the one-meetup-at-a-time rule entirely, High, verified.** `schedule_conflict.go`'s conflict check runs as a plain unlocked SELECT, and both call sites (`CreateMeetup`, `service.go:278`; `RequestToJoin`, `requests.go:32`) run it *before* opening their own separate write transaction — nothing spans the check and the write. Confirmed directly: this is not the narrow same-millisecond race the ADR's Consequences section frames it as; it's reproducible by an ordinary flaky-network retry or two concurrent requests, and it specifically defeats the reason pending requests were made to count (two `RequestToJoin` calls racing past the check both create pending requests, and two hosts accepting independently later reproduces the exact double-booking the feature exists to prevent). **DONE (2026-09-15) — verified directly against source.** `repository.ScheduleGuard`/`ScheduleTx` added; `LockUserSchedule` (`SELECT pg_advisory_xact_lock(hashtext(user_id::text))`) runs as the first statement inside the same transaction `Create` already opens, on the same connection, before the conflict check and the insert — confirmed transaction-scoped (auto-released on commit/rollback, not the session-scoped variant, so no lock leak), and confirmed the service layer's old unlocked pre-check was removed, not left running alongside the new one. New `TestScheduleConflict_ConcurrentCallsSerialize` races real goroutines against the real test-database pool over 6 rounds for both hosting and requesting; confirmed genuine (barrier-released, not sequential) by reading the test directly. Reported control run against the pre-fix code: both concurrent calls won on every round. Post-fix: exactly one wins every round.

40. **The "live meetup" definition (`open`/`full` + `window_end > now()`) is written independently in at least three places with no cross-reference — Medium, verified.** `FindScheduleConflict` (`meetups.sql:535-536`), `ListActiveMeetups`'s `isLive` (`service.go:518-521`), and the related-but-distinct `ListOpenMeetups` browsable filter (`open` only, `meetups.sql:113,185`) all agree today but nothing ties them together, unlike the ST_DWithin-exemption logic elsewhere in the same file which explicitly warns future editors of exactly this drift risk. **DONE (2026-09-15) — verified.** Cross-referencing comments added at all three definitions, naming the other two by location; no SQL refactor, as scoped.

### Frontend

41. **Two low-severity, non-urgent notes from the same pass — tracked, not fixed.** (a) `meetup_card.dart:56`/`meetup_detail_page.dart:258` use the plain `intent.label` rather than the new `intentLabel` (meal-sitting name) in the trust-locked toast — currently harmless since ADR-028 redacts the time in exactly that state, but would silently desync if that redaction rule ever changes. (b) `ScheduleConflictError`'s plain-text fallback doesn't distinguish a pending vs. accepted conflicting request — the real app UI reads the structured field correctly, this only affects a client that ignores it. **OPEN, low priority.**

**Verified clean (no issue found)**: sign-out's background network call (fire-and-forget via a static method with no closure over `this`/`ref`, confirmed it can't crash a disposed notifier; the "skip FCM delete if signed in again" race-avoidance is genuinely implemented and test-covered, not just described) · `MealSitting`'s five time-of-day windows (no gaps/overlaps, not recomputed reactively) · `ScheduleConflictSheet`'s error parsing (single parse site, no duplicate) · `place_search_ui.dart` (no network calls of its own; the actual geocoder debouncing it wraps is untouched) · `OptionalAuth`'s scope (used only by the new logout route, doesn't weaken anything else, no token-validity oracle) · the old `DELETE /v1/meetups/device-token` route (cleanly removed, not orphaned).

## Process note

Most review rounds to date were reactive — triggered by a specific bug report
or a named ask ("check vulnerabilities," "check scalability") — rather than a
recurring full-surface sweep. That's why #1, #2, #4, #5, #6, #11, #12, #13
were never caught earlier: nobody's stated lens ever pointed at them. #7 and
#8 are a different failure mode — real porting gaps from the microservices →
monolith rewrite, which happened phase-by-phase with no parity checklist
against the old repo. Worth considering: a standing periodic full-sweep
(UX + security + parity-vs-old-repo) rather than only reviewing what's asked
about directly.
