<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- "Use my current location" on the Schedule flow's map/location-
         picker step (frontend/meetup-scheduling-PLAN.md's 2026-08-18
         testing addendum) — geolocator requests these at runtime, but the
         manifest declaration is still required. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <application
        android:label="professional_connections_platform"
        android:name="${applicationName}"
        android:icon="@drawable/app_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- LinkedIn OAuth redirect deep link (ADR-011 frontend slice) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="professionalconnections" android:host="auth"/>
            </intent-filter>
            <!-- Prevents the Flutter engine from routing the LinkedIn OAuth
                 deep link (professionalconnections://auth/linkedin/callback)
                 through its own named-route system, which would otherwise
                 throw "Could not find a generator for route" since this app
                 uses plain Navigator.push, not onGenerateRoute. app_links'
                 own channel listener handles the redirect instead. Must be
                 a child of <activity>, not a sibling of it under
                 <application> (Flutter's own docs are explicit about this
                 placement) — it was misplaced there before, which meant it
                 was silently not taking effect at all. -->
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="false" />
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
